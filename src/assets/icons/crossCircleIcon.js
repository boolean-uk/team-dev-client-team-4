import { RxCrossCircled } from 'react-icons/rx';

const CrossCircleIcon = ({ className, onClick }) => {
  return <RxCrossCircled className={className} onClick={onClick} size={26} />;
};
export default CrossCircleIcon;
